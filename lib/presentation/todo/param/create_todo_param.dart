/*
class CreateTodoParam {
  final int id;
  final int userId;
  final String title;
  final bool completed;

  CreateTodoParam(
      {required this.id,
      required this.userId,
      required this.title,
      required this.completed});

  CreateTodoParam copyWith(
      {int? id, int? userId, String? title, bool? completed}) {
    return CreateTodoParam(
      id: id ?? this.id,
      userId: userId ?? this.userId,
      title: title ?? this.title,
      completed: completed ?? this.completed,
    );
  }
}


*/
